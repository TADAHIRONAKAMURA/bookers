<!DOCTYPE html>
<html>
<head>
	<title>WebCampBackendNew</title>
</head>
<body>



	<h1>Books</h1>

	<table>
        <thead>
        	<tr>
        		<th>Title</th>
 			    <th>Body</th>
 			    <th colspan="3"></th>
 		    </tr>
 	    </thead>
 	    <tbody>
 	    	<% @books.each do |book| %>
 	    	<tr>
                <!-- link_to 'リンク文字列', リンク先のパス（URL） -->
                <td><%=book.title%></td>
 			    <td><%=book.body%></td>
 			    <td><%= link_to 'Show', book_path(book.id), class: "show_#{book.id}" %></td> 
	 			<td><%= link_to 'Edit', edit_book_path(book.id), class: "edit_#{book.id}" %></td>
	 			<td><%= link_to 'Destroy', book_path(book.id), class: "destroy_#{book.id}", method: :delete, "data-confirm" =>"Are yuo sure？" %></td>


 		    </tr>
		    <% end %>
 	    </tbody>
     </table>


	<h2>New books</h2>

	<% if @book.errors.any? %>
  <div id="error_explanation">
    <!-- <h2><%= @book.errors.count %>件のエラーがあります。</h2>
 -->
    <ul>
    <% @book.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

	  <%= form_for(@book) do |f| %>

	<div class="field">
	   <!-- <p>Title</p> -->
	    <label for="book_title">Title</label><br>
	    <%= f.text_field :title, class: "book_title" %> 
	</div>

	<div class="field">
	    <!-- <p>Body</p> -->
        <label for="book_body">Body</label><br>
        <%= f.text_area :body, class: "book_body" %>
        <!-- <textarea class="book_body" name="book[body]" id="book_body"></textarea> -->
    </div>
    <div class="field">
        <%= f.submit 'Create Book' %>
    </div>
	    <% end %>
    <!-- バリデーションエラー時のみ、@.errors に値があるので、エラーメッセージを表示する -->
</body>
</html>

